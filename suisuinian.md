# 开发计划

## 核心玩法

1. 待定
2. 待定
3. 待定

## Thinking

该怎么打印多色呢 思考  
~~screen 和 frame 带有颜色层打印单字符时 结合颜色层打印~~  
染色函数  

## future map

### 26-06-21

- [ ] 将`gameFrame.draw`提取出来，单独放置
- [ ] 在`GameScreen`支持彩色绘制
- [ ] 给`picture_class`中支持`color`参数
- [ ] 将牌框与牌面分别保存
- [x] 绘制空格字符串破坏画面，提供不绘制方法
- [ ] 单`picture_class`实例只有单色，多个逐步绘制彩色，构建图层类
- [ ] 创建一个颜色枚举吧
- [ ] 将最外层框用单独一帧绘制，而非固定在`GameScreen`中，外框绘制不一定为矩型？

### 26-06-22

- [x] 修复想要打印`empty`却被跳过输出
- [ ] 将`Picture`中`Layer`的色彩传递到`Screen`中
